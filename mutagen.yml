sync:
  defaults:
    ignore:
      vcs: false
      paths:
        - .DS_Store # macOS files
        - .idea
    permissions:
      defaultFileMode: 644
      defaultDirectoryMode: 755
    # https://mutagen.io/documentation/synchronization#modes
    mode: two-way-resolved
  swoole-bundle:
    alpha: "."
    beta: "docker://app@swoole-bundle-cli-83-swoole-6.2.2/usr/src/app"
