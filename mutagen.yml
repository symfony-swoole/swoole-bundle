sync:
  defaults:
    ignore:
      vcs: false
      paths:
        - .DS_Store # macOS files
        - .idea
    permissions:
      defaultFileMode: 644
      defaultDirectoryMode: 755
    # https://mutagen.io/documentation/synchronization#modes
    mode: two-way-resolved
  swoole-bundle:
    alpha: "."
    beta: "docker://app@swoole-bundle-cli-82-openswoole-25.2.0/usr/src/app"
